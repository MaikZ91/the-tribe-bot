diff --git a/node_modules/whatsapp-web.js/src/structures/Base.js b/node_modules/whatsapp-web.js/src/structures/Base.js
index ed1d83c..12a3430 100644
--- a/node_modules/whatsapp-web.js/src/structures/Base.js
+++ b/node_modules/whatsapp-web.js/src/structures/Base.js
@@ -19,6 +19,23 @@ class Base {
     _patch(data) {
         return data;
     }
+
+    /**
+     * Normalizes a WhatsApp ID object so that `_serialized` is always defined.
+     *
+     * WhatsApp Web renamed the serialized property from `_serialized` to `$1`
+     * in July 2026. Reads that still expect `_serialized` receive undefined,
+     * which surfaces as a minified error thrown inside the page.
+     * @param {object} id
+     * @returns {object}
+     */
+    static _normalizeId(id) {
+        if (!id || typeof id !== 'object') return id;
+        if (id._serialized === undefined && id.$1 !== undefined) {
+            return Object.assign({}, id, { _serialized: id.$1 });
+        }
+        return id;
+    }
 }
 
 module.exports = Base;
diff --git a/node_modules/whatsapp-web.js/src/structures/Chat.js b/node_modules/whatsapp-web.js/src/structures/Chat.js
index 40211bf..79624e3 100644
--- a/node_modules/whatsapp-web.js/src/structures/Chat.js
+++ b/node_modules/whatsapp-web.js/src/structures/Chat.js
@@ -19,7 +19,7 @@ class Chat extends Base {
          * ID that represents the chat
          * @type {object}
          */
-        this.id = data.id;
+        this.id = Base._normalizeId(data.id);
 
         /**
          * Title of the chat
diff --git a/node_modules/whatsapp-web.js/src/structures/Message.js b/node_modules/whatsapp-web.js/src/structures/Message.js
index dd977e8..881ccf1 100644
--- a/node_modules/whatsapp-web.js/src/structures/Message.js
+++ b/node_modules/whatsapp-web.js/src/structures/Message.js
@@ -34,7 +34,7 @@ class Message extends Base {
          * ID that represents the message
          * @type {object}
          */
-        this.id = data.id;
+        this.id = Base._normalizeId(data.id);
 
         /**
          * ACK status for the message
@@ -74,7 +74,7 @@ class Message extends Base {
          */
         this.from =
             typeof data.from === 'object' && data.from !== null
-                ? data.from._serialized
+                ? data.from._serialized || data.from.$1
                 : data.from;
 
         /**
@@ -86,7 +86,7 @@ class Message extends Base {
          */
         this.to =
             typeof data.to === 'object' && data.to !== null
-                ? data.to._serialized
+                ? data.to._serialized || data.to.$1
                 : data.to;
 
         /**
@@ -95,7 +95,7 @@ class Message extends Base {
          */
         this.author =
             typeof data.author === 'object' && data.author !== null
-                ? data.author._serialized
+                ? data.author._serialized || data.author.$1
                 : data.author;
 
         /**
@@ -210,13 +210,13 @@ class Message extends Base {
                       groupName: data.inviteGrpName,
                       fromId:
                           typeof data.from === 'object' &&
-                          '_serialized' in data.from
-                              ? data.from._serialized
+                          ('_serialized' in data.from || '$1' in data.from)
+                              ? data.from._serialized || data.from.$1
                               : data.from,
                       toId:
                           typeof data.to === 'object' &&
-                          '_serialized' in data.to
-                              ? data.to._serialized
+                          ('_serialized' in data.to || '$1' in data.to)
+                              ? data.to._serialized || data.to.$1
                               : data.to,
                   }
                 : undefined;
diff --git a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
index 540078a..dbe0aee 100644
--- a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
+++ b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
@@ -3,6 +3,21 @@
 exports.LoadUtils = () => {
     window.WWebJS = {};
 
+    /**
+     * Reads the serialized form of a WhatsApp ID object.
+     *
+     * WhatsApp Web renamed this property from `_serialized` to `$1` in July
+     * 2026. Passing the resulting undefined into createWid() or Msg.get()
+     * throws inside the page, which reaches the caller as a minified "r" —
+     * the reason getChats() and getChatById() stopped working while
+     * sendMessage() kept going: that one receives the id as a string.
+     */
+    window.WWebJS.serializeId = (id) => {
+        if (!id) return id;
+        if (typeof id === 'string') return id;
+        return id._serialized !== undefined ? id._serialized : id.$1;
+    };
+
     /**
      * Helper function that compares between two WWeb versions. Its purpose is to help the developer to choose the correct code implementation depending on the comparison value and the WWeb version.
      * @param {string} lOperand The left operand for the WWeb version string to compare with
@@ -582,7 +597,7 @@ exports.LoadUtils = () => {
 
         return window
             .require('WAWebCollections')
-            .Msg.get(newMsgKey._serialized);
+            .Msg.get(window.WWebJS.serializeId(newMsgKey));
     };
 
     window.WWebJS.editMessage = async (msg, content, options = {}) => {
@@ -625,7 +640,9 @@ exports.LoadUtils = () => {
         await window
             .require('WAWebSendMessageEditAction')
             .sendMessageEdit(msg, content, internalOptions);
-        return window.require('WAWebCollections').Msg.get(msg.id._serialized);
+        return window
+            .require('WAWebCollections')
+            .Msg.get(window.WWebJS.serializeId(msg.id));
     };
 
     window.WWebJS.toStickerData = async (mediaInfo) => {
@@ -830,10 +847,14 @@ exports.LoadUtils = () => {
 
         if (typeof msg.id.remote === 'object') {
             msg.id = Object.assign({}, msg.id, {
-                remote: msg.id.remote._serialized,
+                remote: window.WWebJS.serializeId(msg.id.remote),
             });
         }
 
+        if (msg.id._serialized === undefined && msg.id.$1 !== undefined) {
+            msg.id = Object.assign({}, msg.id, { _serialized: msg.id.$1 });
+        }
+
         delete msg.pendingAckUpdate;
 
         return msg;
@@ -953,7 +974,7 @@ exports.LoadUtils = () => {
             model.isGroup = true;
             const chatWid = window
                 .require('WAWebWidFactory')
-                .createWid(chat.id._serialized);
+                .createWid(window.WWebJS.serializeId(chat.id));
             const groupMetadata =
                 window.require('WAWebCollections').GroupMetadata ||
                 window.require('WAWebCollections').WAWebGroupMetadataCollection;
@@ -981,16 +1002,15 @@ exports.LoadUtils = () => {
 
         model.lastMessage = null;
         if (model.msgs && model.msgs.length) {
-            const lastMessage = chat.lastReceivedKey
-                ? window
-                      .require('WAWebCollections')
-                      .Msg.get(chat.lastReceivedKey._serialized) ||
+            const lastReceivedId = window.WWebJS.serializeId(
+                chat.lastReceivedKey,
+            );
+            const lastMessage = lastReceivedId
+                ? window.require('WAWebCollections').Msg.get(lastReceivedId) ||
                   (
                       await window
                           .require('WAWebCollections')
-                          .Msg.getMessagesById([
-                              chat.lastReceivedKey._serialized,
-                          ])
+                          .Msg.getMessagesById([lastReceivedId])
                   )?.messages?.[0]
                 : null;
             lastMessage &&
@@ -1263,9 +1283,9 @@ exports.LoadUtils = () => {
     };
 
     window.WWebJS.rejectCall = async (peerJid, id) => {
-        let userId = window
-            .require('WAWebUserPrefsMeUser')
-            .getMaybeMePnUser()._serialized;
+        let userId = window.WWebJS.serializeId(
+            window.require('WAWebUserPrefsMeUser').getMaybeMePnUser(),
+        );
 
         const stanza = window.require('WAWap').wap(
             'call',
@@ -1575,9 +1595,11 @@ exports.LoadUtils = () => {
                                       .membershipRequestsActionRejectParticipantMixins
                                       ?.value.error;
                             return {
-                                requesterId: window
-                                    .require('WAWebWidFactory')
-                                    .createWid(p.jid)._serialized,
+                                requesterId: window.WWebJS.serializeId(
+                                    window
+                                        .require('WAWebWidFactory')
+                                        .createWid(p.jid),
+                                ),
                                 ...(error
                                     ? {
                                           error: +error,
@@ -1594,11 +1616,14 @@ exports.LoadUtils = () => {
                     }
                 } else {
                     result.push({
-                        requesterId: window
-                            .require('WAWebJidToWid')
-                            .userJidToUserWid(
-                                participant.participantArgs[0].participantJid,
-                            )._serialized,
+                        requesterId: window.WWebJS.serializeId(
+                            window
+                                .require('WAWebJidToWid')
+                                .userJidToUserWid(
+                                    participant.participantArgs[0]
+                                        .participantJid,
+                                ),
+                        ),
                         message: 'ServerStatusCodeError',
                     });
                 }
